export default class ShelfCategory {
    private color: string;
    private notes: string;
    private name: string;
    constructor(name: string, color: string, notes: string) {

        this.name = name
        this.color = color
        this.notes = notes

    }
}
